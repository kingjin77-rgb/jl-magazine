# 법무법인 제이엘 하자소송 기본제안서

**재사용 마스터 템플릿.** 고정 25장 + 가변 6장. 단지 정보만 갈아끼워 매번 새 제안서를 낸다.

> **새 세션은 이 파일을 먼저 읽는다.** 그다음 `proposal/README.md` → `proposal/01_제안서_구성안.md` → `proposal/11_브랜드_스타일가이드.md` 순으로 읽으면 전체 맥락이 잡힌다.

---

## 확정 사항 — 바꾸려면 사용자 확인 필요

| 항목 | 값 |
|---|---|
| 제안 주체 | **법무법인 제이엘 단독** (인터로 공동 아님) |
| 분량 | 전체본 31장 + 요약본 10장 (요약본은 전체본의 부분집합) |
| 브랜드 팔레트 | PRIMARY `#385783` · INK `#223D5B` · DARK `#2A3B4F` · MUTED `#8289A3` · CARD `#E9EDF8` · WATERMARK `#C7CEE1` · LINE `#DDE2EC` |
| 경고색 (S5 전용) | 도과 `#C0392B` · 임박 `#E67E22` |
| 서체 | 210 옴니고딕 (폴백 Pretendard) |
| 아트보드 | 16:9 1332×750, 캡처 2배 |

### 실적 귀속 — 절대 혼동 금지

**JL 단독 실적만 쓴다.** 인터로 명의 실적을 JL 실적으로 표기하면 허위 기재다.

- 쓸 수 있음: JL 판결 **18건 · 328.4억 · 9,979세대 · 세대당 평균 3,290,592원** (`proposal/10_JL_수행실적.md`)
- **쓸 수 없음**: 211단지 수임, 313억, 채권양도율 87~99%, 죽림부영 33억 — 전부 인터로 명의

## 톤 규칙 — 예외 없음

- 금지: 시나리오, 골든타임, 구호형 제목, 영어 남용, "약속드립니다", "최고", "유일"
- 표준: 수행 실적, 이행 계획, 검토의견, 대응 체계
- 슬라이드 제목은 **결론 문장형** ("담보책임기간이 2027년 3월 도과합니다")
- **회수 금액 단정 예측 금지. 승소 보장 금지.**
- 변협 광고규정 확인 전까지 **"승소" 표현을 쓰지 않는다** — "수행", "판결" 등 중립 표현
- 권리로 단정 가능한 것과 요청·협의 사항을 구분 표기
- 추정치는 **계산식 병기**. 미확인은 `[미확인]`, 검증 대기는 `[확인]`

## 사실 검증 — AI 거짓 0건

- 수치·인용은 **원문에서 확인한 것만** 쓴다. 기억으로 쓰지 않는다
- 사례를 인용할 때 **제목·수치·층 구성이 서로 맞는지** 확인
  (예: `3.0mm vs 1.82mm`는 옥탑 노출우레탄 사례이지 욕실·발코니 방수가 아니다)
- 판결문 원문과 최소 3건 대조 후 실적 수록

## 이미지 원칙

- 하자 사진·단지 전경·변호사 사진은 **실물만.** AI 생성·스톡 금지
- **웹에서 가져오지 않는다** — 저작권 침해이자 허위표시 (`proposal/12_하자사진_조달방안.md`)
- 하자 사진 업스케일 금지 (없던 디테일이 생성됨). 감정서에서 **원본 해상도로 재추출**
- 타 단지 사진은 **동호수·단지명 마스킹** 후 사용
- 도해는 **SVG로 직접 작성**. 생성 이미지는 치수를 못 맞춘다

## 디렉터리

```
proposal/      00~12 기획 문서. 00=자료요청 01=구성안 10=실적 11=브랜드 12=사진조달
graphics/      SVG 도해 + PNG 렌더
assets/        brand(로고) · lawyers(변호사 사진) · fonts · defect-photos
template/      data(단지 JSON) · slides(고정/가변)
tools/         timeline.py (담보책임기간 자동 계산)
```

## 자주 쓰는 명령

```bash
# 담보책임기간 계산
python3 tools/timeline.py template/data/<단지>.json --asof 2026-08-18

# 한글 폰트 설치 (컨테이너 재생성 시마다)
cp assets/fonts/*.ttf /usr/share/fonts/truetype/jl/ && fc-cache -f

# SVG -> PNG
python3 -c "import cairosvg; cairosvg.svg2png(url='graphics/X.svg', write_to='graphics/X.png', output_width=2400, output_height=1200)"
```

**렌더 후 반드시 PNG를 눈으로 확인한다.** 텍스트 겹침과 두부(□)는 코드만 봐서는 안 보인다.
옴니고딕에 `—`(em dash), `−`(minus)가 없다. `·` `-` 로 대체한다.

## 여러 세션으로 나눠 작업할 때

동시에 같은 파일을 건드리면 충돌한다. **담당 영역을 분리한다.**

| 트랙 | 담당 디렉터리 | 겹치지 않음 |
|---|---|---|
| A 자료 정리 | `proposal/06,09,10,12` · `assets/defect-photos/` | |
| B 그래픽 | `graphics/` | |
| C 본문 문안 | `template/slides/` | |
| D 디자인·캔버스 | `template/slides/` 취합 · 캔버스 | A·B·C 완료 후 |

- 각 트랙은 **자기 브랜치**에서 작업하고 PR로 합친다
- `CLAUDE.md`, `proposal/01`, `proposal/11`은 **공유 기준**이므로 임의로 고치지 않는다. 변경이 필요하면 사용자에게 확인
- 작업 시작 전 `git pull` 필수

## 현재 상태

**완료**: 기획 문서 13종 · 타임라인 계산기 · 브랜드 확정 · 로고/변호사 사진 · JL 실적 18건 · PART 3 도해 4종

**대기 중** (`proposal/00_자료요청서.md` 하단 참조)
1. 하자 사례 사진 — 감정서 PDF를 **대화창에 첨부**하면 원본 해상도로 추출 (드라이브 경유 불가)
2. F-1 착수금 / F-2 성공보수율 / F-5 패소 시 처리
3. C-2 사건번호·단지명 표기 방침 (변협 광고규정)
4. B-4 JL 자체 협력 진단업체

**다음 작업**: 도해 4종(S5 타임라인 · S11 채권양도 · S18 절차 · S24 비용) → 마스터 슬라이드 → Claude Design 캔버스
