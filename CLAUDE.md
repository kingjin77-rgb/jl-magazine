# 법무법인 제이엘 발표자료 제작 규칙

이 문서는 반복 지시를 없애기 위한 것입니다. 작업 전에 반드시 읽고, 아래 규칙을 **묻지 않고 적용**합니다.

## 현재 작업물

`bunyang-conversion/` — 파주 운정 휴아림 분양전환 법률자문 & 단체등기 제안서 (HTML 발표자료)

| 파일 | 용도 |
|---|---|
| `index.html` | 원본 (폴더판, 사진을 `assets/photos/` 에서 읽음) |
| `파주휴아림_분양전환제안서.html` | 배포용 단일 파일 (폰트 base64 내장) — `index.html` 수정 후 반드시 재생성 |
| `verify/original-text.txt` | 원본 PPT 전문. **낱말** 대조의 기준 |
| `verify/original-slides.md` | 원본 60장의 **순서와 장수**의 기준 |
| `qa/` | 검수 스크립트 |

현재 61장. 원본 60장 + 수임실적 집계 2장(요약 · 연도별 그래프).

---

## 절대 원칙 — 어기면 다시 작업

### 1. 내용을 축약하지 않는다
- 원본의 **모든 줄**이 들어가야 한다. 요약·생략·통합 금지.
- 원본의 **파트 순서와 항목 순서**를 지킨다.
- 표현을 바꿀 때도 원본 낱말을 남긴다. (`3천만원` → `3,000만 원` 은 허용, 문장 삭제는 불허)
- 수정 후 `node qa/05-content.js` 를 돌려 **미반영 0** 을 확인한다.
- 검산으로 오류를 찾으면 정정하되, **원안 수치를 취소선으로 병기**하고 사유를 적는다.

### 2. 여백을 만들지 않는다
- 남는 공간이 생기면 **줄 간격을 벌리지 말고** 다음 순서로 해결한다.
  1. 글자를 키운다 (자동 채움 `fitFill()` 이 처리)
  2. 도식·이미지를 키운다
  3. 그 자리에 **정보를 넣는다** (미니 도식, 집계 카드, 눈금선)
- 그리드 컨테이너에는 **반드시** `grid-auto-rows:minmax(0,1fr)` 또는 `grid-template-rows:minmax(0,1fr)` 를 지정한다.
  (미지정 시 이미지가 무한히 커지거나 칸이 위로 몰린다)
- `justify-content:space-evenly` 로 줄을 벌리는 것은 **금지**. 간격이 넓어 보인다는 지적을 받는다.
- **큰 칸에 낱말 하나만 넣는 것도 여백이다.** 칸을 키우는 것만으로는 해결되지 않는다.
  번호 배지 · 계층 연결선 · 부가 수치를 넣어 칸을 채운다.
- 수정 후 `node qa/02-fill.js` 를 돌린다. 두 수치를 본다.
  - **박스 채움률** 95% 이상 (칸이 화면을 덮었는가)
  - **글자 채움률** 14% 이상 (읽을 것이 실제로 있는가) ← 이쪽이 진짜 지표
  숫자 카드·막대그래프 장은 글자 채움률이 낮게 나오는 것이 정상이다.

#### 자동 채움을 쓸 때 반드시 확인할 것
`fitFill()` 은 **컨테이너**에 `font-size` 를 준다. 그러므로 자손에 **고정 px 글자크기가 남아 있으면
아무 효과가 없다.** (`.card ul` 을 키워도 `.card li{font-size:16.5px}` 가 이겨서 본문이 계속 16.5px 이었던
버그가 실제로 있었다. 사용자가 "글자가 작다 · 여백이 많다"고 여러 번 지적한 원인이다.)
- 자동 채움 대상 안의 글자크기는 **모두 `em`** 으로 적는다.
- 대상 컨테이너에 기준 px 을 적는다. 현재 대상:
  `.card ul` 16.5 · `.cmp-body` 16.5 · `.btbl` 16.5 · `.fnode .box ul` 15.8
- `natH()` 는 자식의 **바깥여백까지** 세야 한다. 안 세면 글자를 너무 키워 넘친다.
- 카드가 늘어날 수 있는 구조(`.cmp` 등)에서는 `grid-template-rows:minmax(0,1fr)` 이 없으면
  글자를 키울 때 카드가 같이 커져 무대를 넘는다. 넘침 검사로 반드시 확인한다.

### 3. 투사 가독성
- 본문 **16.5px 이상** (1332×750 무대 기준). 13px 대는 판독 불가.
- 슬라이드 제목 40px, 부제 17.5px, 표 15.8px, 카드 제목 21px.
- 배경은 **흰색**. 다크는 **표지 · 파트 간지 · 마무리**만.
  프로젝터는 검정을 빛을 안 쏘는 것으로 표현해 조명 있는 방에서 대비가 무너지고,
  고령 입주민에게 흰 글자는 번져 보인다.
- `W` 키로 어두운 방 모드 전환을 제공한다 (기본은 밝은 방).

### 4. 모션
- **제목은 출렁이지 않는다.** 단어별 회전·마스크 걷어내기 금지. 제자리 팝업(`scale .962 → 1`)만.
- **목차는 모션 없음.** 진입 시 한 번에 표시.
- **순번이 붙은 항목(01, 02, ①, 1.)은 순번대로 하나씩** 등장. `data-build="1"`.
- **중요 설명 파트**(일시납·분납·국가유공자·월부담·취득세)는 `data-anim="popup"`.
- 업무 흐름도는 **방향성 있게** — 화살표를 넣고 앞 상자와 같은 단계에 등장시킨다(`data-build-with`).
- 전체화면 블러는 쓰지 않는다 (전환 시 드롭 프레임 24 → 0).
- 지난 단계는 흐리게(`G` 로 끔), 현재 단계에 글로우 링.

### 5. 이미지
- 사진은 **자동 배정**한다. 사용자가 하나씩 클릭하게 만들지 않는다.
- **낱말만으로 짝지으면 사람이 뒤바뀐다.** `slideN.xml` 의 도형 좌표를 읽어
  **이름표와 사진의 실제 거리**로 짝짓는다. 화소 크기 순서 · z-order 순서는 읽는 순서와 무관하다.
  - `a:off` / `a:ext` (EMU), `p:grpSp` 의 `chOff`/`chExt` 변환, 표(`a:tbl`) 칸 위치까지 계산한다.
  - 여러 장에 되풀이 나오는 그림(≥4장)은 로고이므로 제외. 아이콘 크기(무대의 5.5% 미만)도 제외.
  - **인물 자리에는 세로 사진만** 넣는다. 없으면 비워 둔다. 엉뚱한 얼굴보다 빈 칸이 낫다.
  - 묶음·잔여 배정도 **원본 슬라이드 읽는 순서**(위→아래, 왼→오른)로 한다.
  - 고칠 때는 `sim2.pptx` 같은 좌표 모사 파일로 검증한다 (z-order 섞고, 화소 크기를 읽는 순서와 역순으로).
    배정 결과는 `window.JLASSIGN` 에 남는다.
- 넣은 이미지는 IndexedDB 에 저장한다. 단 **`file://` 로 열면 크롬·엣지가 저장소를 막는다.**
  그래서 「사진 박은 파일 저장」 단추로 사진이 HTML 안에 박힌 파일을 내려받게 한다.
  사진을 매번 다시 넣게 만들지 않는다.
- 문서(공고문·계약서·증명서류)는 `object-fit:contain` 으로 **잘리지 않게**.
- 사진은 `object-fit:cover`, 인물은 `object-position:center 20%`.
- 모든 이미지는 **클릭하면 전체화면 확대**(휠 확대·끌어 이동). 계약서 설명용.

### 6. 인물 소개
- 지명원 배치를 따른다 — **좌측 사진 / 우측 약력 표**. 색은 흰 배경 유지.
- 약력은 구분 라벨(현직 / 경력 / 자격 / 학력)로 나눈다.
- 여러 명을 나열할 때는 사진이 카드의 남는 높이를 흡수하고, **형제 카드의 사진 밑변을 실측해 정렬**한다.

### 7. 연락처
- 주소·전화·팩스는 **마무리 장에 한 번만**. 소개 장에 반복하지 않는다.
- 예외: 제출 및 증명 서류 장은 증명 목적상 소재지 표기 허용.

### 8. 표현
- 최상급·단정 표현 주의 (변호사 광고규정). "업계 유일", "보장", 느낌표 남용 금지.
- 근거 없는 추정을 단정하지 않는다. 예시 수치에는 `예시` 표기.
- 법률 판단은 원본의 논리를 함부로 뒤집지 않는다. 원본을 오독한 훈계를 붙이지 않는다.
  (원본이 "불가할 경우"라고 쓴 것을 "권장한다"로 읽은 실수가 있었다)

---

### 9. 원본 순서를 지킨다
- 장 순서와 장수의 기준은 `verify/original-slides.md` 다. 파트를 합치거나 옮기지 않는다.
- 원본 목차 장(08 잔금대출 / 09 특별제안 / 10 등기비용)과 본문 간지(08 등기비용 / 09 잔금대출 /
  10 특별제안)가 **원본 자체에서 어긋나 있다.** → **본문 간지 순서를 따르고**, 목차 장에 사유를 적는다.
- 사진만 있는 장(게시판 공지 3장 · 설명회 사진자료 5장 · 증명서류 4장 · 업무협약)도
  **한 장씩 그대로** 만든다. 합치면 지적을 받는다.

---

## 작업 순서

1. `verify/original-text.txt` (낱말) 과 `verify/original-slides.md` (순서) 로 원본을 확인한다.
2. `index.html` 을 수정한다.
3. 검수를 **전부** 돌린다.
   ```bash
   cd bunyang-conversion/qa
   node 01-overflow.js && node 02-fill.js && node 03-steps.js && node 05-content.js
   ```
   - 01 넘침 **0장** · 02 박스 95%↑ 및 글자 14% 미만 최소화 · 03 JS 오류 없음 · 05 미반영 **0**
4. 실제로 **렌더해서 눈으로 본다.** 수치만 보고 넘기지 않는다.
5. 단일 파일을 재생성한다.
   ```bash
   python3 - <<'PY'
   import base64, re, os
   s=open('index.html',encoding='utf-8').read()
   n=s.count('<section class="slide')
   s=re.sub(r'<span id="pg-tot">\d+</span>', f'<span id="pg-tot">{n}</span>', s)
   open('index.html','w',encoding='utf-8').write(s)
   f=base64.b64encode(open('assets/PretendardVariable.woff2','rb').read()).decode()
   open('파주휴아림_분양전환제안서.html','w',encoding='utf-8').write(
     s.replace('src:url("assets/PretendardVariable.woff2") format("woff2-variations");',
               'src:url("data:font/woff2;base64,%s") format("woff2-variations");'%f))
   print(n,'장')
   PY
   ```
6. 커밋 · 푸시 후 단일 파일을 전달한다.

## 검증 없이 "됐습니다" 라고 말하지 않는다

수치를 제시하고, 안 되는 것은 안 된다고 말한다.
파일 전달 경로를 안내할 때는 **사용자 PC 기준**으로 말한다 (클라우드 작업 경로를 말하지 않는다).
